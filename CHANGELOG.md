# Changelog / 更新记录

## R042 public scientific milestone / R042 公开科学里程碑

- Publish the strict bound `s(17) > 115325/24963` with a frozen mixed certificate, independent rational containment audit, and two complete exact replay paths. / 发布严格下界 `s(17) > 115325/24963`，并提供冻结混合证书、独立有理包含审计与两条完整精确复演路径。
- Retarget all 7,853 catalogue rows to parent side `24963/25000` while preserving the interval partition and orientations; 7,848 core-side entries change. / 在保持区间划分与方向不变的情况下，将全部 7,853 个目录行重新定向到父边长 `24963/25000`；其中 7,848 个内核边长条目发生变化。
- Increase threshold-orbit-22 prototype weight by `200000` units, corresponding to a D4-expanded global budget increase of `800000` units. / 将阈值轨道 22 的原型权重增加 `200000` 单位，对应 D4 展开后的全局预算增加 `800000` 单位。
- Record the theorem gate `17 × 999954551 − 16999227356 = 11 > 0` separately from the stronger observed full-replay surplus `811846`. / 将定理门槛 `17 × 999954551 − 16999227356 = 11 > 0` 与更强的全量复演观测余量 `811846` 分开记录。
- Distribute the frozen certificate in deterministic gzip form and reconstruct the secondary BigInt checker from pinned R038 source bytes rather than committing the reconstructed checker. / 以确定性 gzip 形式分发冻结证书，并从锁定的 R038 源字节重建第二套 BigInt 检查器，而不提交重建后的检查器。
- Keep R038, R012, and M19 scientific evidence unchanged. / 保持 R038、R012 与 M19 科学证据不变。

## R038 public scientific milestone / R038 公开科学里程碑

R038 proves `s(17) > 65900000000/14282142857` with a strict parent-side certificate and exact expanded-domain replay. / R038 以严格父边长证书与精确扩域复演证明 `s(17) > 65900000000/14282142857`。

## R012 public scientific milestone / R012 公开科学里程碑

R012 proves `s(17) >= 461300/99999` with exact data, a complete parent-angle proof, and a Python-only full replay. / R012 以精确数据、完整父角证明和纯 Python 完整复演证明 `s(17) >= 461300/99999`。

## Earlier milestones / 早期里程碑

The M19 snapshot retains the nonuniform 197-direction lower bound, M17's exact failure, the complete M12 dependency, and its original replay paths. / M19 快照保留非均匀 197 方向下界、M17 精确失败、完整 M12 依赖及其原始复验路径。
