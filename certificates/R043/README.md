# R043 strict lower bound / R043 严格下界

R043 proves the strict lower bound `s(17) > 461300/99851 = 4.6198836266036394227…`. / R043 证明严格下界 `s(17) > 461300/99851 = 4.6198836266036394227…`。

The certificate uses outer side `4613/1000`, parent side `99851/100000`, 7,853 retargeted catalogue rows, and the R042 mixed point + two-of-three threshold language. / 该证书使用外框边长 `4613/1000`、父边长 `99851/100000`、7,853 个重新定向的目录行，以及 R042 的 point + two-of-three threshold 混合证书语言。

R043 changes no point coordinates and no threshold orbit; it activates four previously zero-weight point orbits on the existing support. / R043 不改变任何 point 坐标或 threshold orbit，只在既有支撑上激活四个此前权重为零的 point orbit。

The final certificate is accepted by two complete exact replay engines plus an independent rational-containment audit. / 最终证书通过两套完整精确复演引擎以及独立有理包含审计验收。

[Proof / 证明](PROOF.md) · [Reproducibility / 可复验性](REPRODUCIBILITY.md) · [Sources / 来源](ATTRIBUTION.md) · [Claims / 结论账本](CLAIMS.json)
